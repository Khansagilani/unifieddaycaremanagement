# NestCare Frontend (Vite + React)

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Default dev server: http://localhost:5173
API base URL: `VITE_API_URL` (defaults to http://localhost:8000)
WebSocket URL: `VITE_WS_URL` (defaults to http://localhost:8000)
